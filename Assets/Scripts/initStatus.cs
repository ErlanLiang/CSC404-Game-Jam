using UnityEngine;

public class initStatus : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //initialize the sliders to 50

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
